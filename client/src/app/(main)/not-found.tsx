import { NotFoundContent } from '@/components/layout'

// Next сам оборачивает этот файл в (main)/layout.tsx (шапка/футер/поиск/
// категории), потому что он лежит внутри группы (main) — сработает на
// notFound() из любого маршрута внутри неё: несуществующее объявление
// (ads/[id]/page.tsx), несуществующая категория каталога и так далее. Для
// URL, вообще не совпавшего ни с одним маршрутом сайта, Next использует
// ТОЛЬКО корневой app/not-found.tsx — вложенные группы туда не участвуют
// (это архитектурное ограничение Next, не наше упущение), поэтому та же
// разметка продублирована и там, но с шапкой/футером, подключёнными вручную.
export default function MainNotFound() {
  return <NotFoundContent />
}
