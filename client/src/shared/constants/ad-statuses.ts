// Подписи статусов объявления — общие для всех мест, где статус
// показывается администратору как текст (карточка модерации, карточка
// пользователя в админке). Значения ключей — строго как в enum AdStatus на
// бэкенде (prisma/schema.prisma). Раньше этот же список был отдельно
// продублирован в AdModerationDetail и в UserAdminDetail — один источник
// правды здесь.
export const AD_STATUS_LABELS: Record<string, string> = {
  DRAFT: 'Черновик',
  PENDING: 'На модерации',
  PUBLISHED: 'Опубликовано',
  REJECTED: 'Отклонено',
  ARCHIVED: 'Снято с публикации',
  EXPIRED: 'Срок действия истёк'
}
