import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // standalone — для докера: сборка кладёт в .next/standalone минимальный
  // самодостаточный сервер (только реально используемые зависимости из
  // node_modules, без dev-хвостов), которого достаточно, чтобы поднять
  // `node server.js` без npm install внутри финального образа. Без этого
  // пришлось бы тащить в рантайм-образ весь node_modules — и по размеру,
  // и по времени сборки/деплоя ощутимо хуже (см. обсуждение с
  // пользователем про перенос на Selectel).
  output: 'standalone',
  // Убирает заголовок X-Powered-By: Next.js из каждого ответа. Версия там не
  // указана, но сам факт «это Next.js» сужает атакующему круг перебора — а
  // пользы от заголовка нет никакой (парный server_tokens off для nginx — в
  // nginx/conf.d/app.conf).
  poweredByHeader: false,
  reactCompiler: process.env.NODE_ENV === 'production',
  env: {
    SERVER_URL: process.env.SERVER_URL,
    GOOGLE_RECAPTCHA_SITE_KEY: process.env.GOOGLE_RECAPTCHA_SITE_KEY
  },
  images: {
    // Хосты, с которых оптимизатору Next разрешено брать исходники картинок.
    // Это не формальность: /_next/image принимает URL параметром, и без
    // белого списка сайт превратился бы в открытый прокси — любой мог бы
    // гонять через наш сервер чужие файлы, расходуя наш трафик и процессор.
    remotePatterns: [
      // Selectel, основное хранилище (бакет agrozone-media, Москва/ru-6).
      // ВАЖНО: это НЕ хост самого S3 API (s3.ru-6.storage.selcloud.ru) —
      // тот у Selectel анонимные запросы не обслуживает вообще, только
      // подписанные, сколько политику доступа ни настраивай (см.
      // обсуждение при миграции с Timeweb). Реальные публичные ссылки
      // FileService строит через отдельный публичный домен бакета
      // (S3_PUBLIC_URL в server/.env, вкладка «Домены» в панели Selectel) —
      // вот он ниже. Домен уникален для этого конкретного бакета, поэтому
      // и без указания пути в pathname более широкий доступ не открывает.
      // Если бакет когда-нибудь пересоздастся — домен изменится, и
      // правку нужно внести и сюда, и в S3_PUBLIC_URL.
      {
        protocol: 'https',
        hostname: '07134e4a-ee55-4355-8233-067990a5e917.selstorage.ru',
        port: '',
        pathname: '/**'
      },
      // Timeweb — старое хранилище. Оставлено намеренно: в базе у уже
      // созданных объявлений лежат ПОЛНЫЕ ссылки на этот хост (см.
      // FileService.uploadFile), и убрать его сейчас — значит сломать
      // картинки во всех существующих объявлениях. Удалять эту запись можно
      // будет только после того, как старые объявления либо переедут, либо
      // будут вычищены вместе с тестовыми данными (см. U10 в ROADMAP.md).
      {
        protocol: 'https',
        hostname: 's3.twcstorage.ru',
        port: '',
        pathname: '/**'
      }
    ],
    // AVIF первым, WebP запасным: браузер сам выберет по Accept-заголовку.
    // AVIF даёт ещё примерно 20–30% поверх WebP на фотографиях техники и
    // продукции, а для нашей аудитории (мобильный интернет в поле) это
    // ощутимее, чем лишние миллисекунды на пережатие — результат всё равно
    // кешируется, см. minimumCacheTTL ниже.
    formats: ['image/avif', 'image/webp'],
    // Год вместо дефолтных 4 часов. Безопасно, потому что оптимизатор
    // кеширует по исходному URL, а имена файлов в S3 уникальны на каждую
    // загрузку (см. FileService на бэкенде): другая картинка — другой URL,
    // так что протухший кеш показать нечего. При 4 часах повторный
    // посетитель заново тянул весь вес страницы.
    minimumCacheTTL: 31536000
  }
}

export default nextConfig
