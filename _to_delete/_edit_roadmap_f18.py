import pathlib

p = pathlib.Path("ROADMAP.md")
s = p.read_text(encoding="utf-8")

old = '- [ ] **F18 · низкий — раздел «Услуги».**'
new = """- [x] **F18 · низкий — раздел «Услуги».**
  Сделано как новая ветка верхнего уровня в дереве категорий (решение
  владельца 08.09.2026), а не отдельная сущность — работает на всей
  существующей инфраструктуре объявлений/каталога/фильтров без изменений
  схемы. Таксономия — 27 категорий 1:1 по образцу agroserver.ru (реального
  работающего конкурента в этой нише, список дал владелец), без
  сокращений: категория в дереве ничего не стоит держать пустой, в отличие
  от F9/F5/F7/F11, где откладывался целый механизм.
  Под каждую категорию — свой набор `categoryFeatures` (14 переиспользуемых
  наборов на 27 категорий, сгруппированы по смыслу: полевые работы,
  перевозки, аренда техники, ремонт, склад/хранение, переработка,
  консультационные услуги и т.д. — по аналогии с тем, как в файле уже
  переиспользуются TECH_ATTACHED/ANIMAL_FEATURES и т.п. на разнородных
  категориях). `PriceUnits` уже подходили под услуги — HA и HOUR в enum
  были явно заложены заранее, просто раздел не был реализован.
  Зона обслуживания (решение владельца) — простое текстовое поле
  `service_area` (`filterable: false`) в характеристиках у каждой
  категории, а не структурированный гео-радиус с доработкой поиска: у
  услуги часто нет фиксированной точки (база + радиус выезда), а не адрес,
  как у товара, но заводить отдельный поиск "услуги, покрывающие точку
  покупателя" (по сути обратная версия F3) — отдельная, гораздо более
  сложная задача, для v1 не нужна.
  Валидация категорий (`validateCategories`) — 0 ошибок, 0 предупреждений
  (нашёл и исправил 1 конфликт типов: `animal_type` уже существовал в
  файле как `MULTI_SELECT` с конкретными опциями — переиспользовал тот же
  тип/опции вместо своего текстового поля). `tsc`/`eslint` — чисто.
  **Осталось:** нужно накатить в БД — `npm run prisma:seed` из `server/`
  (upsert по стабильному id, ничего не удаляет и не ломает существующие
  категории/объявления — безопасно перезапускать)."""

assert s.count(old) == 1
s = s.replace(old, new)
p.write_text(s, encoding="utf-8")
print("OK")
