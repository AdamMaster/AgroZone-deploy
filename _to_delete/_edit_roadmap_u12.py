import pathlib

p = pathlib.Path("ROADMAP.md")
s = p.read_text(encoding="utf-8")

old = '- [ ] **U12 · низкий — компактный баннер, контейнер до 1440 px.**'
new = """- [x] **U12 · низкий — компактный баннер, контейнер до 1440 px.**
  Обе части оказались почти сделаны ещё до этой задачи (аудит проводился
  без доступа к коду, часть выводов устарела — см. "Уточнения" в начале
  файла). Баннер — на главной уже стоит компактный дизайн (`WelcomeBanner`:
  закрываемая полоска `rounded-xl px-4 py-3`, не полноэкранный герой),
  править нечего. Контейнер — общий `<Container>` уже был на 1376px
  (`max-w-344` = 344 × 0.25rem), это единственное место, задающее ширину
  контента на всех страницах; поменял на `max-w-360` = ровно 1440px.
  `tsc`/`eslint` — чисто."""

assert s.count(old) == 1, f"expected 1, got {s.count(old)}"
s = s.replace(old, new)
p.write_text(s, encoding="utf-8")
print("OK")
