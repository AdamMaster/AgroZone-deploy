import pathlib

p = pathlib.Path("ROADMAP.md")
s = p.read_text(encoding="utf-8")

def replace_once(s, old, new):
    assert s.count(old) == 1, f"expected exactly 1 occurrence, got {s.count(old)}: {old[:80]!r}"
    return s.replace(old, new)

old = """  Диагноз — по чтению исходников библиотеки
  (`node_modules/@yandex/smart-captcha/index.js`), не по живому
  воспроизведению — в этой среде нет браузера с реальным доступом к
  скрипту Яндекса. `tsc`/`eslint` — чисто. Проверьте на своей стороне:
  неверный пароль → верный пароль сразу следом должен теперь пускать.

## Этап 0. Быстрые победы (1–2 дня)"""

new = """  Диагноз — по чтению исходников библиотеки
  (`node_modules/@yandex/smart-captcha/index.js`), не по живому
  воспроизведению — в этой среде нет браузера с реальным доступом к
  скрипту Яндекса. `tsc`/`eslint` — чисто. Проверьте на своей стороне:
  неверный пароль → верный пароль сразу следом должен теперь пускать.
- [x] **Уведомления о новых сообщениях (найдено при скоупинге F17, не сам F17).**
  При оценке F17 (телеграм-бот/канал) выяснилось: новое сообщение в диалоге
  по объявлению вообще не порождало никакого уведомления получателю — ни
  email, ни in-app-записи, только realtime по WS, если получатель именно в
  этот момент держит открытой вкладку "Мои сообщения". На практике —
  тихая потеря части сделок: написали продавцу, а он узнает об этом только
  если сам зайдёт проверить. Решено чинить отдельно от Telegram-бота
  (сам F17 отложен — см. ниже) — это база, которая нужна независимо от
  канала уведомлений.
  Сделано по образцу уже существующего `notifyAdRejected`: добавлен
  `NotificationType.NEW_MESSAGE`, `NotificationsService.notifyNewMessage`
  (in-app-запись создаётся всегда; письмо — best-effort, в try/catch, и
  только если у пользователя вообще есть email — **часть пользователей
  регистрируется только по номеру телефона и email не имеет**, см.
  `User.email` в схеме), письмо — новый шаблон
  `new-message.template.tsx` + `MailService.sendNewMessageEmail`.
  `ConversationsService.createMessage` — единая точка создания сообщения
  (используется и `startConversation`, и `sendMessage`) — теперь после
  записи сообщения вызывает `notifyNewMessage`, тоже в try/catch: сбой
  уведомления не должен ронять отправку самого сообщения. Фронтенд —
  просто расширен union-тип `NotificationType` (рендер уведомлений и так
  полностью generic по `title`/`message`/`link`, без switch по типу).
  Требует `npx prisma generate && npx prisma migrate dev` (новое значение
  enum в схеме).

## Этап 0. Быстрые победы (1–2 дня)"""

s = replace_once(s, old, new)

# Small note on the F17 line clarifying the split.
old2 = "- [ ] **F17 · средний — Telegram-бот и канал.**"
new2 = ("- [ ] **F17 · средний — Telegram-бот и канал.** Отложено по решению\n"
        "  владельца (07.09.2026): сайт только начинает жить, аудитория канала\n"
        "  пока не наберётся, а бот-уведомления как отдельная ценность выделены\n"
        "  и уже сделаны без Telegram (см. \"Уведомления о новых сообщениях\" во\n"
        "  внеплановых доработках выше) — возврат к самому Telegram-боту/каналу\n"
        "  по инициативе владельца при реальном росте.")
s = replace_once(s, old2, new2)

p.write_text(s, encoding="utf-8")
print("OK")
